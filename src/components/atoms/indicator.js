import React, { useEffect } from "react";
import { StyleSheet, View } from 'react-native';
import Animation from 'lottie-react-native';
import LoadingView from '../customeLoader'
const Indicator = (props) => {
    return <LoadingView />
};
export default Indicator


